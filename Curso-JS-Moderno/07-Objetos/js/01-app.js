// Crear objetos en JavaScript

const nombre = "monitor de 20 pulgadas";
const precio = 300;
const disponible = true;

/* Un objeto agrupa todo en una sola variable,
una forma de reconocer un objeto es porque sus valores están entre llaves, object literal,
se declara cada variable(llave) y luego de los dos puntos colocamos su valor
*/

// Object literal
const producto = {
  nombre: "monitor 20 pulgadas",
  precio: 300,
  disponible: true,
};

console.log(producto);
